version: '3.0' 

services:
  webserver:
    image: node22
    networks:
      - project-network
    volumes:
      - project-volume:/projects
    ports:
      - "3000:3000"
    command: [ "npm", "start" ]

volumes:
  project-volume:
    external: true
    name: ${VOLUME_NAME}

networks:
  project-network:
    external: true
    name: ${NETWORK_NAME}